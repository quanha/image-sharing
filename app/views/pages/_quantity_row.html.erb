<% if product_quantity.present? %>
  <% product_quantity.each do |item| %>
    <tr>
      <td width="90%"><%= item.store.name %></td>
      <td> <%= item.quantity %></td>
    </tr>
  <% end %>
<% end %>